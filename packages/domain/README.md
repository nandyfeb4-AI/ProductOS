# Domain Package

Shared ProductOS domain concepts belong here.

Examples:
- initiatives, features, stories, and PRD abstractions
- decision traceability models
- workflow policies
- reusable business services for future agents and jobs

For the MVP, most implementation still lives in the API app, but this package creates the right long-term boundary.

