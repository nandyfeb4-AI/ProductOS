# Contracts Package

Shared API contracts and cross-application payload definitions live here.

The current contract is still documented in `docs/api-contract.md`. As the platform matures, request and response models should move into this package so the API app, web app, and future workers all consume the same contract layer.

